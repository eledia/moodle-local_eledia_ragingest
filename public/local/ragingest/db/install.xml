<?xml version="1.0" encoding="UTF-8" ?>
<XMLDB PATH="local/ragingest/db" VERSION="20260613" COMMENT="eLeDia.ai RagIngest plugin schema."
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:noNamespaceSchemaLocation="../../../lib/xmldb/xmldb.xsd"
>
  <TABLES>
    <TABLE NAME="local_ragingest_course" COMMENT="Tracks whether each course is currently represented in the RAG index, so that toggling a course's ingestion marking can trigger a one-time re-index (on enable) or purge (on disable) instead of acting on every course edit.">
      <FIELDS>
        <FIELD NAME="id" TYPE="int" LENGTH="10" NOTNULL="true" SEQUENCE="true"/>
        <FIELD NAME="courseid" TYPE="int" LENGTH="10" NOTNULL="true" SEQUENCE="false" COMMENT="The course."/>
        <FIELD NAME="ingested" TYPE="int" LENGTH="1" NOTNULL="true" DEFAULT="0" SEQUENCE="false" COMMENT="1 when the course is currently believed to be in the RAG index."/>
        <FIELD NAME="timemodified" TYPE="int" LENGTH="10" NOTNULL="true" SEQUENCE="false" COMMENT="Last state change."/>
      </FIELDS>
      <KEYS>
        <KEY NAME="primary" TYPE="primary" FIELDS="id"/>
        <KEY NAME="courseid" TYPE="foreign-unique" FIELDS="courseid" REFTABLE="course" REFFIELDS="id" COMMENT="One state row per course."/>
      </KEYS>
    </TABLE>
  </TABLES>
</XMLDB>
